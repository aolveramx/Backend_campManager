# CampManager Back-end

#### Start Server in development mode:

```ssh
npm run dev
```

#### Seeder functionality:

##### Import data:

```ssh
node seeder -i
```

##### Destroy data:

```ssh
node seeder -d
```
